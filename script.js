const kilometri = prompt("inserisci il chilometraggio")
const soldi =  ( 0.21 * parseInt(kilometri) )
console.log(soldi)
const eta = prompt("inserisci la tua eta!")
if (eta <= 17){
  const sconto = math.floor((soldi/20)*100)
  console.log(`il costo totale del tuo biglietto è: ${sconto}€`)
}
else if (eta >= 65){
    const scontoOver = Math.floor(soldi/)
}
